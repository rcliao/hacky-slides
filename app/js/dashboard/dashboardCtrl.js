/*global define*/

define(
	[
		'hackySlides.module'
	],
	function (app) {
		'use strict';

		DashboardCtrl.$inject =
			[
			];

		return app
			.register
			.controller('DashboardCtrl', DashboardCtrl);

		function DashboardCtrl (
		) {

		}
	}
);